# 🏗️ PROJECT ARCHITECTURE

## System Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                   DATA SOURCES                                  │
│  (CSV, API, Kaggle, Synthetic Data)                            │
└────────────────────┬────────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────────┐
│                   ETL LAYER                                     │
│  • Data Loading (csv, api, database)        [src/etl/]         │
│  • Cleaning (missing values, outliers)                         │
│  • Normalization & encoding                                    │
└────────────────────┬────────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────────┐
│              FEATURE ENGINEERING                                │
│  • Recency features (days_since_purchase)  [src/features/]     │
│  • Frequency features (purchase_count)                         │
│  • Monetary features (total_value)                             │
│  • Engagement scores (visits, email opens)                     │
└────────────────────┬────────────────────────────────────────────┘
                     │
          ┌──────────┴──────────┐
          │                     │
          ▼                     ▼
    ┌──────────────┐      ┌──────────────┐
    │ CHURN MODEL  │      │ MARKETING    │
    │              │      │ RESPONSE     │
    │ XGBoost      │      │               │
    │              │      │ LightGBM     │
    │ ROC-AUC:.85  │      │ ROC-AUC:.80  │
    │ Recall:.75   │      │ Precision:.6 │
    └──────┬───────┘      └──────┬───────┘
           │                     │
           │    Predictions      │
           └──────────┬──────────┘
                      │
                      ▼
    ┌─────────────────────────────────────┐
    │  BUSINESS METRICS ENGINE            │
    │                                     │
    │  • Expected Profit Calculation      │
    │  • Customer Lifetime Value (CLV)    │
    │  • Customer Segmentation            │
    │  • ROI Analysis                     │
    └────────────┬────────────────────────┘
                 │
        ┌────────┴────────┬────────────┐
        │                 │            │
        ▼                 ▼            ▼
    ┌────────┐      ┌──────────┐  ┌─────────┐
    │ EXPLAI │      │ AI ASSIS-│  │ METRICS │
    │ NABILITY│      │ TANT     │  │ & KPI   │
    │ (SHAP) │      │ (LLM)    │  │         │
    │        │      │          │  │         │
    │Feature │      │Business  │  │Segments │
    │Import  │      │recom-    │  │Profit   │
    │        │      │mend-     │  │Risk     │
    └────────┘      │ations    │  └─────────┘
                    └──────────┘
                         │
                         ▼
    ┌─────────────────────────────────────┐
    │  DASHBOARD / API                    │
    │                                     │
    │  • Streamlit UI (dashboard/app.py)  │
    │  • FastAPI Endpoints (deployment)   │
    │  • Real-time predictions            │
    │  • Interactive visualizations       │
    │  • AI chat interface                │
    └─────────────────────────────────────┘
```

---

## Data Flow

```
CUSTOMER DATA (CSV/DB)
        │
        ▼
    ┌─────────────────────────────────────┐
    │ DATA LOADING & VALIDATION           │
    │ src/etl/data_loader.py              │
    │ ─────────────────────────────────   │
    │ • CSV to DataFrame                  │
    │ • Handle missing values             │
    │ • Remove outliers                   │
    │ • Validation checks                 │
    └────────────┬────────────────────────┘
                 │ (cleaned_data)
                 ▼
    ┌─────────────────────────────────────┐
    │ FEATURE ENGINEERING                 │
    │ src/features/feature_engineer.py    │
    │ ─────────────────────────────────   │
    │ • RFM Features (Recency,            │
    │   Frequency, Monetary)              │
    │ • Engagement metrics                │
    │ • One-hot encoding                  │
    │ • Interaction features              │
    └────────────┬────────────────────────┘
                 │ (feature_matrix)
                 ▼
    ┌──────────────────────────────────────────────┐
    │ MODEL TRAINING                               │
    │ src/models/                                  │
    │ ──────────────────────────────────────────   │
    │ ┌─────────────────┐  ┌─────────────────┐   │
    │ │ Churn Model     │  │ Marketing Model │   │
    │ │ (churn_model.py)│  │ (marketing_model│   │
    │ │                 │  │ .py)            │   │
    │ │ X_train, y_churn│  │ X_train, y_resp │   │
    │ │      │          │  │      │          │   │
    │ │      ▼          │  │      ▼          │   │
    │ │   XGBoost       │  │   LightGBM      │   │
    │ │      │          │  │      │          │   │
    │ │      ▼          │  │      ▼          │   │
    │ │  P(churn)       │  │ P(response)     │   │
    │ └─────────────────┘  └─────────────────┘   │
    └──────────┬──────────────────┬───────────────┘
               │                  │
               └────────┬─────────┘
                        │
                        ▼
    ┌─────────────────────────────────────┐
    │ PREDICTIONS                         │
    │ ─────────────────────────────────   │
    │ For each customer:                  │
    │ • P(churn₁, churn₂, ..., churnₙ)   │
    │ • P(resp₁, resp₂, ..., respₙ)      │
    └────────────┬────────────────────────┘
                 │
                 ▼
    ┌─────────────────────────────────────┐
    │ BUSINESS METRICS                    │
    │ src/metrics/business_metrics.py     │
    │ ─────────────────────────────────   │
    │ Expected Profit = Σ P(response)     │
    │                  × AOV - Cost       │
    │                                     │
    │ Churn Impact = Σ P(churn) × CLV    │
    │                                     │
    │ Segments: VIP, At-Risk,             │
    │           Prospect, Lost            │
    └────────────┬────────────────────────┘
                 │
        ┌────────┴────────┐
        │                 │
        ▼                 ▼
    ┌────────────┐   ┌────────────────┐
    │ SHAP       │   │ AI ASSISTANT   │
    │ Explain    │   │ Recommend      │
    │ Why        │   │ Actions        │
    └────────────┘   └────────────────┘
        │                 │
        └────────┬────────┘
                 │
                 ▼
    ┌─────────────────────────────────────┐
    │ DASHBOARD / REPORTS                 │
    │ dashboard/app.py                    │
    │ ─────────────────────────────────   │
    │ • Real-time metrics                 │
    │ • Customer segments                 │
    │ • Profit forecasts                  │
    │ • AI recommendations                │
    └─────────────────────────────────────┘
```

---

## Module Dependencies

```
data_loader.py
    ├── Provides: DataLoader, data cleaning
    └── Used by: Feature engineering, Training

feature_engineer.py
    ├── Requires: Cleaned DataFrame
    ├── Provides: Engineered features
    └── Used by: Model training, Predictions

churn_model.py
    ├── Requires: Features + churn target
    ├── Provides: Churn predictions & probabilities
    ├── Used by: Business metrics, Explainability
    └── Saves: models/churn_model.pkl

marketing_model.py
    ├── Requires: Features + response target
    ├── Provides: Response probabilities
    ├── Used by: Business metrics, Explainability
    └── Saves: models/marketing_model.pkl

business_metrics.py
    ├── Requires: P(churn), P(response)
    ├── Provides: Profit, ROI, Segments
    └── Used by: Dashboard, AI assistant

shap_explainer.py
    ├── Requires: Trained models + data
    ├── Provides: SHAP values, Feature importance
    └── Used by: Dashboard, Reports

llm_assistant.py
    ├── Requires: Business metrics
    ├── Provides: Text recommendations
    └── Used by: Dashboard, Reports

dashboard/app.py
    ├── Requires: All modules above
    ├── Provides: Interactive UI
    └── Entry point: streamlit run dashboard/app.py
```

---

## Database Schema (Optional Future Enhancement)

```
CUSTOMERS
├── customer_id (PK)
├── age, region
├── registration_date
├── tenure_days
└── ...

TRANSACTIONS
├── transaction_id (PK)
├── customer_id (FK)
├── date, amount
└── category

CAMPAIGNS
├── campaign_id (PK)
├── campaign_date
├── type (email, push, etc)
├── budget
└── ...

PREDICTIONS
├── prediction_id (PK)
├── customer_id (FK)
├── churn_probability
├── response_probability
├── created_at
└── ...

METRICS
├── metric_id (PK)
├── customer_id (FK)
├── predicted_ltv
├── churn_risk
├── segment
└── ...
```

---

## Deployment Architecture (Week 3)

```
┌─────────────────────────────────┐
│  Docker Container               │
├─────────────────────────────────┤
│                                 │
│  ┌─────────────────────────┐   │
│  │ FastAPI Server          │   │
│  │ /predict                │   │
│  │ /metrics                │   │
│  │ /explain                │   │
│  └──────────┬──────────────┘   │
│             │                  │
│  ┌──────────┴──────────────┐   │
│  │ Loading Models          │   │
│  │ • churn_model.pkl       │   │
│  │ • marketing_model.pkl   │   │
│  └──────────┬──────────────┘   │
│             │                  │
│  ┌──────────┴──────────────┐   │
│  │ Business Logic          │   │
│  │ (All ML components)     │   │
│  └─────────────────────────┘   │
│                                 │
└────────────┬────────────────────┘
             │
        ┌────┴────┐
        │          │
        ▼          ▼
   Frontend   External APIs
   (React)    (OpenAI, DB)
```

---

## Development Workflow

```
Local Development
        │
        ├─► Create feature branch
        │       │
        │       ├─► Write code
        │       │
        │       ├─► Test locally
        │       │
        │       └─► Push to GitHub
        │
        ├─► Code review
        │
        ├─► Merge to main
        │
        └─► Deploy to staging/production
                │
                ├─► Run tests
                │
                ├─► Monitor metrics
                │
                └─► Production ready ✓
```

---

## Performance Targets

```
MODEL PERFORMANCE
├── Churn Model
│   ├── ROC-AUC: >= 0.85 ✓
│   ├── Recall: >= 0.75 ✓
│   └── Inference time: < 100ms
│
└── Marketing Model
    ├── ROC-AUC: >= 0.80 ✓
    ├── Precision: >= 0.60 ✓
    └── Inference time: < 100ms

BUSINESS METRICS
├── Churn reduction: 3-5% annually
├── Marketing ROI improvement: 40-60%
├── Profit lift: 12-18%
└── Cost reduction: 8-12%

INFRASTRUCTURE
├── Dashboard load time: < 2 seconds
├── API response time: < 500ms
├── Data refresh: Real-time or hourly
└── Uptime: 99.5%+
```

---

**Last Updated**: 2026-02-07  
**Version**: 0.1.0-MVP
